import { MockRendererFactory2 } from './mock-renderer-factory2';

describe('MockRendererFactory2', () => {
  it('should create an instance', () => {
    expect(new MockRendererFactory2()).toBeTruthy();
  });
});
