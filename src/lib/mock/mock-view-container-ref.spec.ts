import { MockViewContainerRef } from './mock-view-container-ref';

describe('MockViewContainerRef', () => {
  it('should create an instance', () => {
    expect(new MockViewContainerRef()).toBeTruthy();
  });
});
