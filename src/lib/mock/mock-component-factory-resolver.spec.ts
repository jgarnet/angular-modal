import { MockComponentFactoryResolver } from './mock-component-factory-resolver';

describe('MockComponentFactoryResolver', () => {
  it('should create an instance', () => {
    expect(new MockComponentFactoryResolver()).toBeTruthy();
  });
});
